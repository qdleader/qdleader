# 📋 发布前检查清单

在运行 `npm publish` 之前，请确保完成以下检查：

## 🔐 账号和权限

- [ ] 已注册 npm 账号
- [ ] 已运行 `npm login` 并登录成功
- [ ] 运行 `npm whoami` 确认登录用户
- [ ] 已验证邮箱（npm 会发送验证邮件）

## 📦 包信息

- [ ] `package.json` 中的 `name` 字段唯一且可用
  - 运行 `npm view qdleader-mcp` 检查是否已被占用
  - 如被占用，考虑使用 `@username/qdleader-mcp` 格式
- [ ] `version` 字段正确（初次发布为 `1.0.0`）
- [ ] `description` 字段描述清晰
- [ ] `keywords` 字段包含相关关键词
- [ ] `author` 字段已填写
- [ ] `license` 字段为 `MIT`
- [ ] `repository` 字段指向正确的 GitHub 仓库
- [ ] `homepage` 和 `bugs` 字段已设置

## 📝 文档

- [ ] `README.md` 内容完整，包含：
  - [ ] 项目介绍
  - [ ] 安装说明
  - [ ] 使用示例
  - [ ] API 文档
  - [ ] 配置示例
- [ ] `LICENSE` 文件存在
- [ ] `CHANGELOG.md` 已创建并记录初始版本

## 🔨 代码质量

- [ ] 代码已提交到 Git
- [ ] 运行 `npm install` 成功
- [ ] 运行 `npm run build` 成功
- [ ] `dist/` 目录包含编译后的文件
- [ ] `dist/index.js` 文件存在且可执行
- [ ] 检查 `dist/index.js` 第一行是否有 shebang: `#!/usr/bin/env node`

## 🗂️ 文件配置

- [ ] `.gitignore` 正确配置（排除 `node_modules/`, `dist/`, etc.）
- [ ] `.npmignore` 正确配置（排除 `src/`, `tsconfig.json`, etc.）
- [ ] 运行 `npm publish --dry-run` 检查将要发布的文件
- [ ] 确认只包含必要文件：
  - ✅ `dist/`
  - ✅ `README.md`
  - ✅ `LICENSE`
  - ✅ `package.json`
  - ❌ `src/` (不应包含)
  - ❌ `node_modules/` (不应包含)

## 🧪 本地测试

- [ ] 运行 `npm link` 创建本地链接
- [ ] 在测试项目中运行 `npm link qdleader-mcp`
- [ ] 测试基本功能：
  - [ ] `search_knowledge` 工具
  - [ ] `read_document` 工具
  - [ ] `list_categories` 工具
  - [ ] `get_category_contents` 工具
- [ ] 确认能正确索引文档
- [ ] 确认搜索返回正确结果

## 📊 包大小

- [ ] 运行 `npm pack` 查看包大小
- [ ] 确认包大小合理（应该 < 1MB）
- [ ] 如果过大，检查是否包含了不必要的文件

## 🔍 最终检查

- [ ] 再次运行 `npm run build` 确保最新代码已构建
- [ ] 检查 `package.json` 中的 `bin` 字段指向正确
- [ ] 确认所有依赖都在 `dependencies` 而不是 `devDependencies`
- [ ] 运行 `npm publish --dry-run` 最后一次确认

## 📢 发布准备

- [ ] 准备好发布公告文本
- [ ] 准备在社交媒体分享
- [ ] 准备更新主仓库 README

## ✅ 准备就绪！

如果以上所有项都已完成，你可以运行：

```bash
npm publish --access public
```

或使用自动化脚本：

```bash
./publish.sh
```

## 🎉 发布后

- [ ] 访问 https://www.npmjs.com/package/qdleader-mcp 确认发布成功
- [ ] 测试安装: `npx -y qdleader-mcp`
- [ ] 更新 GitHub README 添加 npm 徽章
- [ ] 创建 GitHub Release
- [ ] 在社交媒体宣布
- [ ] 在技术社区分享

## 🚨 如果出现问题

### 发布失败

1. 检查网络连接
2. 确认已登录 npm
3. 检查包名是否被占用
4. 查看错误信息并根据提示修复

### 需要撤销发布

```bash
npm unpublish qdleader-mcp@1.0.0
```

⚠️ 注意：只能在发布后 72 小时内撤销

### 需要更新已发布的包

```bash

# 修改代码
# 构建项目

npm run build

# 更新版本

npm version patch

# 重新发布

npm publish
```

---

**祝发布顺利！** 🚀
