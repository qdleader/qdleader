# 蓝湖mcp

```js
{
  "mcpServers": {
    "lanhu-developer-mcp": {
      "command": "npx",
      "args": ["-y", "lanhu-developer-mcp", "--stdio"],
      "env": {
        "LANHU_TOKEN": "<your-lanhu-token>"
      }
    }
  }
}
```

token 就是你蓝湖的token，用cookie里面的就行


这个mcp不是很好用，有待完善